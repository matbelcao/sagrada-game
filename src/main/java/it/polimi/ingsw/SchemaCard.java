package it.polimi.ingsw;

public class SchemaCard extends Card {

    /**
     * Initialize the common parameters
     *
     * @param name   name of the card
     * @param imgSrc directory path of the image
     * @param id     unique id of the card
     */
    public SchemaCard(String name, String imgSrc, int id) {
        super(name, imgSrc, id);
    }
}
