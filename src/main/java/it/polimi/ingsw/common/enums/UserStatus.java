package it.polimi.ingsw.common.enums;

/**
 * This enum represents the status that an user can assume. The elements are immutable.
 */
public enum UserStatus {
    LOBBY,
    PLAYING,
    DISCONNECTED,
    CONNECTED,
}
